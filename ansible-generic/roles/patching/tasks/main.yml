---
# =============================================================================
# Role: patching — Apply security updates on Linux and Windows hosts
# =============================================================================

- name: Run OS-specific patching tasks
  ansible.builtin.include_tasks: "{{ 'windows' if ansible_os_family == 'Windows' else 'linux' }}.yml"
  when: patch | default(true)
